# K9s Plugin: Chronos Pod Scheduling Decision
# Shows scheduling decision for a specific pod

# plugins:
  # chronos-decision:
  #   shortCut: Shift-W
  #   description: "Show Chronos scheduling decision for this pod"
  #   scopes:
  #   - po
  #   command: bash
  #   background: false
  #   args:
  #   - -c
  #   - |
  #     echo "📱 Analyzing live scheduler logs for pod: $NAME"
  #     ~/.config/k9s/plugins/chronos-pod-decision.sh "$NAME"
      
  #     echo -e "\n💡 Press any key to return to k9s..."
  #     read -n 1 -s -r -p ""
plugins:
  chronos-decision:
    shortCut: w
    description: "Show Chronos scheduling decision for this pod"
    scopes:
    - po
    command: bash
    background: false
    args:
    - -cx
    - |
      echo "📱 Analyzing live scheduler logs for pod: $NAME (ns: $NAMESPACE)"
      $HOME/.config/k9s/plugins/chronos-pod-decision.sh "$NAME" "$NAMESPACE" || echo "❌ Script failed"

      echo -e "\n💡 Press any key to return to k9s..."
      read -n 1 -s -r -p ""
